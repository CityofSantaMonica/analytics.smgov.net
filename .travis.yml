language: ruby
rvm:
  - 2.2
script:
  - chmod +x build.sh
  - ./build.sh
before_deploy:
  - git add -f _site/ # Deployments respect `.gitignore`, so _site wouldn't be deployed
deploy:
  provider: azure_web_apps
  skip_cleanup: true
env:
  global:
  - NOKOGIRI_USE_SYSTEM_LIBRARIES=true
